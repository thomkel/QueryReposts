<%= form_for(@repost) do |f| %>
  <% if @repost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@repost.errors.count, "error") %> prohibited this repost from being saved:</h2>

      <ul>
      <% @repost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
